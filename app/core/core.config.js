(function () {
  'use strict';

  angular.module('app.core').config(CoreConfig);

  CoreConfig.$inject = [];
  function CoreConfig() {
      // Config normally goes in here, but my app is way to small to need any. hehe :)
  }
})();
